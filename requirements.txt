# Dependencias de runtime del bot (ver requirements-dev.txt para los tests).
websockets      # feed de precios: subscribeTokenTrade de PumpPortal
requests        # Lightning Transaction API de PumpPortal
solana          # lecturas on-chain (getAccountInfo / getProgramAccounts)
solders         # tipos de Solana (Pubkey, Signature) usados por solana-py
pumpswapamm     # decodificación de las cuentas de pool de PumpSwap
